import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'v6-firebase-tabs-auth',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
